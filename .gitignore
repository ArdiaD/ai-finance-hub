.venv/
__pycache__/
*.pyc
*.egg-info/
.env
linkedin/

# All working data — the database, PDF archive (incoming/library), and Excel
# files — lives under data/ and is never committed. The published corpus lives
# in docs/papers.json, which IS committed.
data/

# Local scratch / OS junk — never publish
.DS_Store
_other/
# FAME grant proposal + notes — private, never publish
fame/

# Local editor/preview config (machine-specific paths)
.claude/
